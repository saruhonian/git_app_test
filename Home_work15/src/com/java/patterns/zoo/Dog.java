package com.java.patterns.zoo;

public class Dog extends Zoo {
    Dog(String name, int age, String breed, String color, int weight) {
        setName(name);
        setAge(age);
        setBreed(breed);
        setColor(color);
        setWeight(weight);
    }
}
