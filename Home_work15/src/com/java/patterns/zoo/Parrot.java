package com.java.patterns.zoo;

public class Parrot extends Zoo {
    Parrot(String name, int age, String breed, String color, int weight) {
        setName(name);
        setAge(age);
        setBreed(breed);
        setColor(color);
        setWeight(weight);
    }
}
