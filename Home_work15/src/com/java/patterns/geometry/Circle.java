package com.java.patterns.geometry;

public class Circle extends GeometricModel {
    Circle(String name, String color, int perimeter) {
        setName(name);
        setColor(color);
        setPerimeter(perimeter);
    }
}
