package com.java.patterns.geometry;

public class Rectangle extends GeometricModel {
    Rectangle(String name, String color, int perimeter) {
        setName(name);
        setColor(color);
        setPerimeter(perimeter);
    }
}
